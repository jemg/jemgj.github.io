{
  "name": "jsonapi",
  "version": "1.0.0",
  "description": "To fetch data from database and convert it to a json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jemin George Jose",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "sqlite3": "^4.0.2"
  }
}
